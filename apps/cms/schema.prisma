// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String    @id @default(cuid())
  name      String    @default("")
  email     String    @unique @default("")
  password  String
  createdAt DateTime? @default(now())

  @@map("keystone_user")
}

model AppUser {
  id            String    @id @default(cuid())
  user_id       String    @default("")
  provider      String    @default("")
  provider_id   String    @default("")
  email         String    @default("")
  name          String    @default("")
  role          String    @default("")
  created_time  DateTime? @default(now())
  modified_time DateTime? @default(now())

  @@map("app_user")
}

model FavoriteStore {
  id       String @id @default(cuid())
  user_id  String @default("")
  store_id String @default("")

  @@map("favorite_store")
}

model Keyword {
  id             String         @id @default(cuid())
  keyword_id     String         @unique @default("")
  keyword_name   String         @default("")
  keyword_type   String         @default("")
  store_keywords StoreKeyword[] @relation("StoreKeyword_keyword")

  @@map("keyword")
}

model StoreKeyword {
  id         String   @id @default(cuid())
  store      Store?   @relation("StoreKeyword_store", fields: [storeId], references: [id])
  storeId    String?  @map("store")
  keyword    Keyword? @relation("StoreKeyword_keyword", fields: [keywordId], references: [id])
  keywordId  String?  @map("keyword")
  store_id   String   @default("")
  keyword_id String   @default("")

  @@index([storeId])
  @@index([keywordId])
  @@map("store_keyword")
}

model Store {
  id             String         @id @default(cuid())
  store_id       String         @unique @default("")
  name           String         @default("")
  address        String         @default("")
  latitude       Float?
  longitude      Float?
  contact_number String         @default("")
  business_hours String         @default("")
  store_keywords StoreKeyword[] @relation("StoreKeyword_store")

  @@map("store")
}

model SuggestedStore {
  id           String @id @default(cuid())
  name         String @default("")
  location     String @default("")
  description  String @default("")
  website      String @default("")
  businessHour String @default("")

  @@map("suggested_store")
}
